<% if has_body_params? -%>
$site = $client->sites->clone(
    <%= quote(url_params['site']) %>,
    <%= php_array(body_params) %>
);
<% else -%>
$site = $client->sites->clone(<%= quote(url_params['site']) %>);
<% end -%>