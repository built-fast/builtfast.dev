<% if has_query_params? -%>
$response = $client->sites->getLogs(
    <%= quote(url_params['site']) %>,
    <%= php_array(query_params) %>
);
<% else -%>
$response = $client->sites->getLogs(<%= quote(url_params['site']) %>);
<% end -%>