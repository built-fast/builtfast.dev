<% if has_body_params? -%>
const exportSession = await client.sites.db.createExport(
    <%= quote(url_params['site']) %>,
    <%= js_object(body_params) %>
);
<% else -%>
const exportSession = await client.sites.db.createExport(<%= quote(url_params['site']) %>);
<% end -%>
